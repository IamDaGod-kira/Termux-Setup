echo "Now interface will be run to install your OS" && echo "Now you will find a number of os names and their alias . to install one , type {proot-distro install <alias>}" && proot-distro list && echo "Now type the alias of the OS you want & enter"
